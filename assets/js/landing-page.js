/**
 * Landing page entry.
 *
 * The quote-form submit handler that used to live here now belongs
 * to the main `app` bundle (see assets/js/quote-form.js). This entry
 * is preserved so its CSS payload -- currently just the shared
 * body-background image -- remains available under
 * encore_entry_link_tags('landing-page') if a template opts in.
 */
import '../css/landing-page.scss';
